apiVersion: v1
kind: Service
metadata:
  name: demand-forecast-api
  labels:
    app: demand-forecast-api
spec:
  type: ClusterIP
  selector:
    app: demand-forecast-api
  ports:
    - name: http
      port: 80
      targetPort: 8000